@using System.Collections
@using Domain
@model Domain.Person
@{
    ViewData["Title"] = "Edit";
}

<h4>Your Personal Info</h4>
<div>

</div>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="FamilyId" />
            <input type="hidden" asp-for="AppUserId"/>
            <input id="logo-input" type="hidden" asp-for="Logo"/>
            <div>
                <img id="logo-img" src="@ViewBag.Logo" alt="logo" width="64" height="64" onclick="changeLogo()" />
            </div>
            <div class="form-group">
                <label asp-for="FirstName" class="control-label"></label>
                <input asp-for="FirstName" class="form-control" />
                <span asp-validation-for="FirstName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LastName" class="control-label"></label>
                <input asp-for="LastName" class="form-control" />
                <span asp-validation-for="LastName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PersonType" class="control-label"></label>
                <select asp-for="PersonType" asp-items="@Html.GetEnumSelectList<PersonType>()" class="form-control"></select>
                <span asp-validation-for="PersonType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

<div id="logo-div" style="display: none ">
    <table class="table">
        @{ var i = 0; }
        @foreach (string logo in (IEnumerable) ViewData["Logos"])
        {
            if (i == 10 || i == 0)
            {
                <tr></tr>
                i = 0;
            }
            <td>
                <img src="/Icons/@logo" alt="logo" width="64" height="64" onclick="chooseLogo('/Icons/@logo')" />
            </td>
            i++;
        }
    </table>
    <a type="button"  style="position: absolute; right: 10px; top: 5px; color: white" onclick="new function() {
      logoDiv.style.display='none';
    }">X</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}




